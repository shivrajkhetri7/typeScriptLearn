// ! Question to check the even/odd number

// * Write a function to check the Odd or even number

function isEven(a: number): boolean {
  return a % 2 === 0;
}

console.log(isEven(5));

// TODO write a function pass argument as a number and check is is divisible by 4 and 8 and return boolean value

function isNumberDiv(a: number): boolean {
  return a % 4 == 0 && a % 8 == 0;
}

console.log(isNumberDiv(20));

//************************************************/
// ? BIG INT
//************************************************/
